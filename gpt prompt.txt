Follow the project docs as the source of truth. Do not invent architecture that conflicts with them. Write future-proof, data-driven, modular code with clear responsibilities, reusable systems, and configurable definitions for content like items, processes, buildings, tasks, and NPC data. Prefer clean structures over shortcuts or hardcoding. Keep implementation aligned with the technical specs, naming, enums, schemas, and progression rules already established. When uncertain, reconcile with the documentation and/or user before changing direction. Add short explanations as comments in the code. Don't use inferred variables but explicit.

Give me exact instructions, give me code blocks (preferably whole functions or scripts) to put in the scripts and explain what we're doing each step in the chat. Don't send patch files or script files. Send me the code in the chat preferably embedded in a scrollable text window (should be default for you already) so I can copy and paste the code directly into Godot.